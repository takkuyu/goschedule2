import 'package:flutter/material.dart';

class OurDashboard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
        child: Text("Dashboard", textScaleFactor: 2.0,)
    );
  }
}

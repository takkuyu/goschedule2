import 'package:flutter/material.dart';

class OurCreateShifts extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
        child: Text("Create Shifts", textScaleFactor: 2.0,)
    );
  }
}
